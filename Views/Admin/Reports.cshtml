@{
    ViewData["Title"] = "Raporlar";
}

<div class="container-fluid py-4">
    <!-- Başlık ve Breadcrumb -->
    <div class="row mb-4">
        <div class="col-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-action="Dashboard">Admin Panel</a></li>
                    <li class="breadcrumb-item active">Raporlar</li>
                </ol>
            </nav>
            <h2 class="fw-bold">
                <i class="fas fa-chart-bar text-info me-2"></i>
                Raporlar & İstatistikler
            </h2>
            <p class="text-muted">Sistem genelindeki istatistikleri ve raporları görüntüleyin</p>
        </div>
    </div>

    <!-- Son 30 Gün Özeti -->
    <div class="row g-3 mb-4">
        <div class="col-12">
            <h5 class="text-muted mb-3"><i class="fas fa-calendar-alt me-2"></i>Son 30 Gün</h5>
        </div>
        <div class="col-xl-4 col-md-4">
            <div class="card border-0 shadow-sm bg-gradient" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                <div class="card-body text-white py-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h3 class="mb-0 fw-bold">@ViewBag.NewUsersLast30Days</h3>
                            <small>Yeni Kullanıcı</small>
                        </div>
                        <i class="fas fa-user-plus fa-2x opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-md-4">
            <div class="card border-0 shadow-sm bg-gradient" style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);">
                <div class="card-body text-white py-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h3 class="mb-0 fw-bold">@ViewBag.NewJobsLast30Days</h3>
                            <small>Yeni İlan</small>
                        </div>
                        <i class="fas fa-briefcase fa-2x opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-md-4">
            <div class="card border-0 shadow-sm bg-gradient" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);">
                <div class="card-body text-white py-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h3 class="mb-0 fw-bold">@ViewBag.NewApplicationsLast30Days</h3>
                            <small>Yeni Başvuru</small>
                        </div>
                        <i class="fas fa-file-alt fa-2x opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <!-- Kullanıcı İstatistikleri -->
        <div class="col-xl-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-users me-2"></i>Kullanıcı İstatistikleri</h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Toplam Kullanıcı
                            <span class="badge bg-primary rounded-pill">@ViewBag.TotalUsers</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Öğrenci
                            <span class="badge bg-info rounded-pill">@ViewBag.TotalStudents</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Firma
                            <span class="badge bg-success rounded-pill">@ViewBag.TotalCompanies</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Aktif Kullanıcı
                            <span class="badge bg-secondary rounded-pill">@ViewBag.ActiveUsers</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- İlan İstatistikleri -->
        <div class="col-xl-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0"><i class="fas fa-briefcase me-2"></i>İlan İstatistikleri</h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Toplam İlan
                            <span class="badge bg-primary rounded-pill">@ViewBag.TotalJobs</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Onay Bekleyen
                            <span class="badge bg-warning text-dark rounded-pill">@ViewBag.PendingJobs</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Onaylanan
                            <span class="badge bg-success rounded-pill">@ViewBag.ApprovedJobs</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Reddedilen
                            <span class="badge bg-danger rounded-pill">@ViewBag.RejectedJobs</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Aktif İlan
                            <span class="badge bg-info rounded-pill">@ViewBag.ActiveJobs</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Başvuru İstatistikleri -->
        <div class="col-xl-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0"><i class="fas fa-file-alt me-2"></i>Başvuru İstatistikleri</h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Toplam Başvuru
                            <span class="badge bg-primary rounded-pill">@ViewBag.TotalApplications</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Bekleyen
                            <span class="badge bg-warning text-dark rounded-pill">@ViewBag.PendingApplications</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Kabul Edilen
                            <span class="badge bg-success rounded-pill">@ViewBag.AcceptedApplications</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Reddedilen
                            <span class="badge bg-danger rounded-pill">@ViewBag.RejectedApplications</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4 mt-2">
        <!-- En Çok Başvuru Alan İlanlar -->
        <div class="col-xl-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white">
                    <h5 class="mb-0"><i class="fas fa-trophy text-warning me-2"></i>En Çok Başvuru Alan İlanlar</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>#</th>
                                    <th>İlan</th>
                                    <th>Firma</th>
                                    <th class="text-center">Başvuru</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int rank = 1;
                                    var topJobs = ViewBag.TopJobs as IEnumerable<dynamic>;
                                }
                                @if (topJobs != null)
                                {
                                    @foreach (var item in topJobs)
                                    {
                                        <tr>
                                            <td>
                                                @if (rank == 1) { <span class="badge bg-warning text-dark">🥇</span> }
                                                else if (rank == 2) { <span class="badge bg-secondary">🥈</span> }
                                                else if (rank == 3) { <span class="badge bg-danger">🥉</span> }
                                                else { @rank }
                                            </td>
                                            <td class="text-truncate" style="max-width: 200px;" title="@item.Job.Title">
                                                @item.Job.Title
                                            </td>
                                            <td class="text-truncate" style="max-width: 150px;">
                                                @item.Job.Company?.CompanyName
                                            </td>
                                            <td class="text-center">
                                                <span class="badge bg-primary">@item.ApplicationCount</span>
                                            </td>
                                        </tr>
                                        rank++;
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- En Aktif Firmalar -->
        <div class="col-xl-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white">
                    <h5 class="mb-0"><i class="fas fa-building text-primary me-2"></i>En Aktif Firmalar</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>#</th>
                                    <th>Firma</th>
                                    <th>Sektör</th>
                                    <th class="text-center">İlan Sayısı</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int companyRank = 1;
                                    var topCompanies = ViewBag.TopCompanies as IEnumerable<dynamic>;
                                }
                                @if (topCompanies != null)
                                {
                                    @foreach (var item in topCompanies)
                                    {
                                        <tr>
                                            <td>
                                                @if (companyRank == 1) { <span class="badge bg-warning text-dark">🥇</span> }
                                                else if (companyRank == 2) { <span class="badge bg-secondary">🥈</span> }
                                                else if (companyRank == 3) { <span class="badge bg-danger">🥉</span> }
                                                else { @companyRank }
                                            </td>
                                            <td class="text-truncate" style="max-width: 200px;">
                                                @item.Company?.CompanyName
                                            </td>
                                            <td class="text-truncate" style="max-width: 150px;">
                                                @(item.Company?.Industry ?? "-")
                                            </td>
                                            <td class="text-center">
                                                <span class="badge bg-success">@item.JobCount</span>
                                            </td>
                                        </tr>
                                        companyRank++;
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
